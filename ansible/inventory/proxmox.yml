plugin: community.proxmox.proxmox
url: "{{ lookup('env', 'PROXMOX_URL') }}"
user: "{{ lookup('env', 'PROXMOX_USER') }}"
token_id: "{{ lookup('env', 'PROXMOX_TOKEN_ID') }}"
token_secret: "{{ lookup('env', 'PROXMOX_TOKEN_SECRET') }}"
want_facts: true
want_proxmox_nodes: true
keyed_groups:
  - key: proxmox_tags_parsed
    separator: ""
# compose:
#   ansible_host: >-
#     (proxmox_agent_interfaces 
#     | default([]) 
#     | rejectattr('name', 'equalto', 'lo') 
#     | map(attribute='ip-addresses') 
#     | flatten 
#     | ansible.utils.ipv4 
#     | first 
#     | default(inventory_hostname)).split('/') 
#     | first