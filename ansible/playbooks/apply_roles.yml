---
- name: Apply roles
  hosts: all
  remote_user: ansible
  become: true

  vars:
      ansible_ssh_extra_args: "-o StrictHostKeyChecking=accept-new"

  roles:
    - set_timezone
    - manage_ansible_user
